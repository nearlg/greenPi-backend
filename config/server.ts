export const PORT: number = Number(process.env.PORT) || 8080;
export const NAME: string = process.env.SERVER_NAME || 'greenpi-backend';
export const VERSION: string = process.env.API_VERSION || '0.0.0';
